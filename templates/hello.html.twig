{% extends "base.html.twig" %}

{% block javascripts %}

 
{% endblock %}

{% block body %}

    <h1>Nos produits phares</h1>
    <div class='row'>
        {% for product in products %}
        <div class="col">
            <div class="card">
                <img src="{{ product.mainPicture }}" class="img-fluid" alt='chaise en bois'>
                <div class='card-body'>
                    <h4 class="card-title">{{ product.name }} ({{ product.price/100 }}€)</h4>
                    <p class='card-text'>{{ product.shortDescription }}</p>
                    <a href="" class="btn btn-primary btn-sm">Détails</a>
                    <a href="" class="btn btn-success btn-sm">Ajouter</a>
                </div>
            </div>
        </div>
        {% endfor%}
    </div>
    <hr>
    <h2>Un service irreprochable</h2>
    <p>Lorem ipsum dolor sit, amet consectetur adipisicing elit. Fuga, nemo.</p>

    <div class="row">
        <div class="col">
            <div class="card bg-primary text-white text-center">
                <div class="card-body">
                    <i class="fas fa-truck fa-2x mb-3"></i>
                    <p class="card-text">Lorem ipsum dolor sit amet consectetur adipisicing elit. Mollitia doloribus officiis asperiores corporis porro molestias.</p>
                </div>
            </div>
        </div>
</div>
{% endblock %}
